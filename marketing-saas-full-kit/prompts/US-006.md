# Codex prompt — US-006

> Paste CONTEXT.md into this session first, then paste this prompt.

---

Read the full story in `stories/US-006-*.md` for all acceptance criteria, data model, and scope details.

## Before starting
Confirm the following are already implemented:
- US-001 through US-005 (dashboard and date range picker component must exist)

## Implementation instructions

1. **Tests first** — Write a test for every acceptance criterion (AC1–AC6) in the story file before writing any implementation code.

2. **Implementation** — Build route handlers, service layer, and frontend components to pass all tests.

3. **Key details** — Chart data endpoint groups metricRecord by date. Use daily granularity for ≤90 day ranges, weekly for longer. Gaps in data (no records for a day) must render as line breaks, not interpolated zeros. Prior period series is nullable.

## Do not build
- Hourly granularity
- Custom metric formulas
- Forecasting/projections
