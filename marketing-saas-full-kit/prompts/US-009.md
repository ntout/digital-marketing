# Codex prompt — US-009

> Paste CONTEXT.md into this session first, then paste this prompt.

---

Read the full story in `stories/US-009-*.md` for all acceptance criteria, data model, and scope details.

## Before starting
Confirm the following are already implemented:
- US-005, US-006, US-007 (dashboard data and charts to populate report)

## Implementation instructions

1. **Tests first** — Write a test for every acceptance criterion (AC1–AC6) in the story file before writing any implementation code.

2. **Implementation** — Build route handlers, service layer, and frontend components to pass all tests.

3. **Key details** — PDF generation runs server-side (use a headless renderer). Charts must be rendered as images server-side before embedding in PDF. ReportSchedule model needed in Prisma. Scheduled sending uses BullMQ with a repeatable job.

## File structure to create
```
packages/db/prisma/schema.prisma              (add ReportSchedule model)
apps/worker/src/jobs/report-delivery.job.ts
apps/worker/src/services/ReportService.ts
apps/web/src/app/api/v1/reports/generate/route.ts
apps/web/src/app/api/v1/reports/schedules/route.ts
apps/web/src/app/api/v1/reports/schedules/[id]/route.ts
apps/web/src/__tests__/reports.test.ts
```

## Do not build
- Custom report builder
- White-labeling
- Slack delivery
