# Codex prompt — US-007

> Paste CONTEXT.md into this session first, then paste this prompt.

---

Read the full story in `stories/US-007-*.md` for all acceptance criteria, data model, and scope details.

## Before starting
Confirm the following are already implemented:
- US-001 through US-006 (campaign data and date range picker)

## Implementation instructions

1. **Tests first** — Write a test for every acceptance criterion (AC1–AC6) in the story file before writing any implementation code.

2. **Implementation** — Build route handlers, service layer, and frontend components to pass all tests.

3. **Key details** — Campaign table queries metricRecord grouped by (campaignId, adSetId). Expandable rows for ad sets. Sortable columns. CSV export streams directly from the server — do not load all rows into memory first.

## Do not build
- Ad-level (individual creative) breakdown
- Inline campaign name editing
