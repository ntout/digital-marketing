# Codex prompt — US-005

> Paste CONTEXT.md into this session first, then paste this prompt.

---

Read the full story in `stories/US-005-*.md` for all acceptance criteria, data model, and scope details.

## Before starting
Confirm the following are already implemented:
- US-001 (auth), US-002 (roles), US-003 (connections), US-004 (metric data synced)

## Implementation instructions

1. **Tests first** — Write a test for every acceptance criterion (AC1–AC6) in the story file before writing any implementation code.

2. **Implementation** — Build route handlers (in `apps/web/src/app/api/v1/`), service layer (in `apps/web/src/lib/services/`), and frontend components to pass all tests.

3. **Key details** — Dashboard API aggregates metricRecord server-side. No new DB tables. Compute % change vs prior period in the service layer, not the frontend. Share the date range picker component — it will be reused in US-006, US-007, US-009.

## Do not build
- Customizable widget layout
- Goal/target overlays
- Real-time streaming
